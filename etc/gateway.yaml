rootPath: .
database:
  redis:
    standalone:
      host: localhost
      port: 6379
    options:
      db: 1
gateway:
  protocol:
    https:
      listenPort: 443
      privateKey: './key/privkey.pem'
      certificate: './key/fullchain.pem'